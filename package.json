{
  "name": "vico-site",
  "private": true,
  "description": "Static site for VICO — dev tooling only (accessibility tests). The site itself has no build step.",
  "scripts": {
    "serve": "python3 -m http.server 8791",
    "test:a11y": "playwright test",
    "pa11y": "pa11y-ci",
    "lighthouse": "lhci autorun"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.48.0",
    "pa11y-ci": "^3.1.0"
  }
}
